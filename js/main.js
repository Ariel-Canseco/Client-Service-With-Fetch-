/*
const main = () =>{
    const multiplicar =(multiplicando, multiplicador) =>{
        console.log(multiplicando*multiplicador);
    };

    multiplicar(6,9);
};
*/

//main();
/*
(() =>{
    const multiplicar =(multiplicando, multiplicador) =>{
    console.log(multiplicando*multiplicador);
};

multiplicar(6,9);
})();


(() =>{
    const chicharronera =(a, b, c) =>{
    //console.log(multiplicando*multiplicador);
    var d;
    if(a==0){

        var x1=(-c)/b;
        var x2=0;
    }else{

        d=b*b-4*a*c;

        if(d>0){

            var x1=(-b+(Math.sqrt(d)))/(2*a);
            var x2=(-b-(Math.sqrt(d)))/((2*a));
        }

        if(d==0){

            var x1=(-b)/2*a;
            var x2=(-b)/2*a; 
        }

        if(d<0){
            console.log("Raices imaginarias");
        }
    }

    console.log("Soluciones: "+ "x1="+ x1 +" "+ "x2=" + x2);
};

chicharronera(2,-7,3);
})();

*/
/*
(()=>{

    console.log('Objeto',utils.factorial(4));
})();
*/

(() => {
    /*console.log('suma',utils.sumar(3,5));
    console.log('resta',utils.restar(6,3));
    console.log('factorial',utils.factorial(5));
    console.log('dividir',utils.dividir([5,2]));
    console.log('dividir',utils.dividir([5,2]));
    const fnCallback = (a,b) => {
        const sumaCuadrados = Math.pow(a,2) + Math.pow(b,2);
        return Math.sqrt(sumaCuadrados);
    };
    console.log('operacion fn -> ',utils.operar(3,5,utils.sumar));
    console.log('operacion fn -> ',utils.operar(3,5,fnCallback));
    */
    })();